<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jsppractice.mapper.UserMapper">
	<select id="findByEmail"
		resultType="com.example.jsppractice.model.User">
		SELECT *
		FROM users u
		WHERE u.email = #{email}
	</select>

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO users (email, name, password_hash, role)
		VALUES (#{email},
		#{name}, #{passwordHash}, #{role})
	</insert>

	<update id="update">
		UPDATE users
		SET name = #{name},
		password_hash =
		#{passwordHash}, role = #{role}
		WHERE id =
		#{id}
	</update>
</mapper>